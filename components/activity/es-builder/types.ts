import { EsStatus, EsAnswer, EsChatSession, EsChatMessage, EsQuestionTemplate, EsScoreResult } from '@/types';

export type { EsStatus, EsAnswer, EsChatSession, EsQuestionTemplate, EsScoreResult };
export type ChatMessage = EsChatMessage;
