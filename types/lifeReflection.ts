export type LifeReflectionQuestionContext = {
  questionId: string;
  questionText: string;
  answer?: string;
  eraId?: string;
  episodeId?: string;
  episodeTitle?: string;
  episodeAge?: string;
};
